The code reads a corpus and produce TF-IDF vectors for documents in the corpus. Then, for a given query string it returns
the document with the highest cosine similarity socre for the query.

The corpus is the trasncripts of all the general election presidential debates from 1960 to 2012.

